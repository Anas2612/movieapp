import React from "react";
import Header from "../../common/header/Header";


const Details = (props) => {
    return (
        <div>
            <Header showBookShowButton={true} {...props}></Header>
        </div>
    )
}